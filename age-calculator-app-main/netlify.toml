# age-calculator/netlify.toml

[build]
  command = "npm run build"  # Adjust based on your build command
  publish = "."              # Adjust based on your build output directory

[[redirects]]
  from = "/*"
  to = "/age-calculator/:splat"
  status = 200
  force = true
